[project]
name = "knowledge-core"
version = "0.1.0"
description = "Executable skeleton for a source-to-wiki knowledge pipeline"
requires-python = ">=3.9"

[project.scripts]
knowledge = "knowledge_core.cli:main"
